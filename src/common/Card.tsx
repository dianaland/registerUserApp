import * as React from "react";
import './Card.css';

interface OwnProps {
}

const Card: React.FunctionComponent<OwnProps> = () => {
  return (
      <div className={"Card-root"}>
      </div>
    );
};

export default (Card);
